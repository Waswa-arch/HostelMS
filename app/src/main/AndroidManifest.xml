<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HostelMS"
        android:usesCleartextTraffic="true">

        <!-- Splash (unchanged) -->
        <activity android:name=".activities.SplashActivity"
            android:exported="true"
            android:noHistory="true"
            android:theme="@style/Theme.HostelMS.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Auth (updated) -->
        <activity android:name=".activities.RoleSelectActivity" android:exported="false"/>
        <activity android:name=".activities.RegisterActivity" android:exported="false"/>
        <activity android:name=".activities.LoginActivity" android:exported="false"/>

        <!-- Student (original kept + new added) -->
        <activity android:name=".activities.StudentDashboardActivity" android:exported="false"/>
        <activity android:name=".activities.StudentProfileActivity" android:exported="false"/>
        <activity android:name=".activities.QrScanActivity" android:exported="false"/>
        <activity android:name=".activities.CheckInActivity" android:exported="false"/>
        <activity android:name=".activities.ComplaintActivity" android:exported="false"/>
        <activity android:name=".activities.AnnouncementsActivity" android:exported="false"/>

        <!-- NEW: Hostel flow (Booking + Inquiry) -->
        <activity android:name=".activities.HostelListActivity" android:exported="false"/>
        <activity android:name=".activities.HostelDetailActivity" android:exported="false"/>
        <activity android:name=".activities.BookRoomActivity" android:exported="false"/>
        <activity android:name=".activities.BookingConfirmationActivity" android:exported="false"/>

        <!-- NEW: Room checklist (post-booking inspection) -->
        <activity android:name=".activities.RoomChecklistActivity" android:exported="false"/>

        <!-- NEW: Student info screens -->
        <activity android:name=".activities.FaqActivity" android:exported="false"/>
        <activity android:name=".activities.HowToUseActivity" android:exported="false"/>

        <!-- Admin (original kept) -->
        <activity android:name=".activities.AdminDashboardActivity" android:exported="false"/>
        <activity android:name=".activities.ManageRoomsActivity" android:exported="false"/>
        <activity android:name=".activities.AddEditRoomActivity" android:exported="false"/>
        <activity android:name=".activities.ManageStudentsActivity" android:exported="false"/>
        <activity android:name=".activities.AddEditStudentActivity" android:exported="false"/>
        <activity android:name=".activities.AttendanceReportActivity" android:exported="false"/>
        <activity android:name=".activities.ManageComplaintsActivity" android:exported="false"/>
        <activity android:name=".activities.AddAnnouncementActivity" android:exported="false"/>
        <activity android:name=".activities.QrGenerateActivity" android:exported="false"/>
        <activity android:name=".activities.AllocateRoomActivity" android:exported="false"/>

    </application>
</manifest>